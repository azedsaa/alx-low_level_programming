#include "main.h"
/**
  * add - adds two integers and returns the result.
  * @int : values to be added
  * Return: result
  */
int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}
