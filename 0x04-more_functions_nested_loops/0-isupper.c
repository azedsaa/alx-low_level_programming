#include <stdio.h>
#include "main.h"
/**
  * _isupper - function is the letter uppercase
  * @c: the carachter to print
  * Return: on success 1.
  */

int _isupper(int c)
{
{
	if (isupper(c))
		return (1);
}
{
	else
		return (0);
}
}
