#include <stdio.h>

/**
  * main - function print on digit  numbers
  * @x: base 10
  * Return: always 0.
  */
int main(void)
{
	int x = 0;

	while (x < 10)
{
		printf("%d\n", x);
		x++;
}
	return (0);
}
